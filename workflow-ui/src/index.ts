/** Host entrypoint for the browser-only workflow workspace plugin. */
export const name = 'workflow-ui'

/** The feature is discovered through the dsh.client export. */
export function apply(): void {}
